Client:
1. transaction history shows transactions but under my accounts in recent transactions nothing is shown
2. when verification code expires front needs to make a new challenge and user needs to enter the new code
3. loans displayed incorrectly, no information about installments, interest rate, more details about loan not loading(it should use data already on front when loans api was hit)
4. transfer and payment history should only show completed transactions or show pending separately (status column missing)
5. commission on transactions and payments before verification are not showing(backend problem but front only hits exchange api)
6.
Employee:
1. Employee cant make a card under different name for a company account(check spec)
2. Client limits view not working(error seen before)
3. Check if interest rates and bank margins are added to the installment(fix is needed for client to be able to confirm this)