- In EmployeeForm (for editing employee data and new employee creation) change gender column from text field to dropdown menu with options: Male, Female, Other, Misha.
- Change the EmployeeFilters text field so it works the following way:
1. The filter component should first have a dropdown menu with filtration categories (First Name, Last Name, Email, Position)
2. Next to the dropdown menu there should be a text field inside of which user can enter the input for filter
3. At the end of the text field there should be an X option to delete current filter and reset table with original data.
4. Based on the filter category and user input, the EmployeeTable should be filtered to show inputs that match the given filter category and input
5. Filtering should be done locally. This means that results for filter search should be done on the current data that exists, and if input field is empty, consider it as having no filter applied (all data should be shown)
- Make the browser icon a little dollar sign
- Implement and Format CSS of the components the following way:
1. Use best practice for CSS Frontend formatting
2. Use Frontend-Development Claude skill
3. The design should be as follows:
1. Colorscheme should be a mix of deep blue(main color), white(background), jade green(accent color 1), fuschia(accent color 2)
2. Edges should be a little rounded, but still keep form
3. Overall look should be professional, this means that colors should be in harmony, accent colors used where it makes sense
4. On each screen, the main component should stand out the most, it can be accented by size and color
5. The layout of components needs to have a good flow and be easy to follow and tell apart